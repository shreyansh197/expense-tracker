rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // App has migrated to PostgreSQL. Deny all Firestore access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
